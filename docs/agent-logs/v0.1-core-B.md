# v0.1 · 绑定层 + 应用整合 task（Task B）

**派活时间**：2026-07-13  
**目标**：wailsbindings + cmd/mossterm + config + secret 真实实现

## 任务描述

- `config.Manager` 完整实现（CRUD + Save/Load + 默认值填充）
- `config.loader.go` 路径解析 + 复制示例
- `secret/keyring.go` 系统 keyring + unlockedCache
- `secret/file.go` AES-256-GCM + Argon2id 加密文件 fallback
- `agent.MemoryRegistry` 跳板链 registry（v0.1 空）
- `wailsbindings/api.go` 全部 12 个方法从 panic 改真实
- `app/app.go` EventEmitter interface + wailsEmitter
- `cmd/mossterm/main.go` 启动流程串起来

## 关键设计决策

- **config.Manager 路径归一化**：MkdirAll + 0o700 权限
- **secret 双后端**：优先 keyring，降级加密文件
- **argon2id 参数**：OWASP 2024 推荐（time=2, memory=64MB, threads=4）
- **wailsEmitter 适配**：避免 wails 包被反向引用（接口抽象）
- **sshAdapter**：connect.Factory 签名适配（Go 严格类型）

## 交付物

- 9 个 .go 文件（config/manager.go 344 行 / secret/file.go 476 行）
- 启动流程：parseFlags → logger → cfg → sec → ag → reg → mm → app → wails

## commit / tag

- **commit**：`44d5824`
- **tag**：`v0.0` `v0.1` `v0.1.1`

## review 发现

- **secret 行数 925 > 500 红线**：CI 没卡，v0.2 拆 secretindex 子包（暂搁置）
- 完整启动流程，日志结构清晰
- Wails 绑定方法 ctx 规范
