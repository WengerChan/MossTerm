# v0.0 · 架构师 task

**派活时间**：2026-07-13  
**目标**：MossTerm 项目架构设计文档

## 任务描述

- 设计 MossTerm 整体架构（Go + Wails 开源 SSH 客户端）
- 输出 `docs/ARCHITECTURE.md`（1364 行，6 个 Mermaid 图）
- 14+ 个 Go interface 硬契约（Connector / Session / Manager / AuthMethod）
- MVP 边界（v0.1 必含 / 不含）
- 风险与对策

## 关键设计决策

- **5 层架构**：Frontend ↔ Wails Bridge ↔ Backend Core ↔ Connectors ↔ OS
- **协议无关设计**：`connect.Connector` 接口 + Registry 模式
- **事件驱动 + 状态机**：Session 6 状态转换，events 通道 + fanout
- **依赖注入**：Manager 通过 With*() 注入器拿到 Deps，避免循环依赖

## 交付物

- `docs/ARCHITECTURE.md`（1364 行 / 38 KB / 6 Mermaid 图）
- 14+ Go interface 契约（Versioned）
- MVP 验收标准 7 条

## commit / tag

- **commit**：`44d5824` (v0.0+v0.1+v0.1.1 合并 commit)
- **tag**：`v0.0` `v0.1` `v0.1.1`（指向同一 commit）

## review 发现

- 架构覆盖完整，6 个 Mermaid 图清晰
- 接口命名 `authMethod()`（私有方法 sum-type）是 Go 惯用法
- 数据流章节给出 SSH 连接完整生命周期图
